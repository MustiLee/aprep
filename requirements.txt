# Core Dependencies
anthropic>=0.21.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# PDF Processing (for CED Parser)
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# Mathematical Processing
sympy>=1.12
numpy>=1.24.0
scipy>=1.11.0

# Data Processing
pyyaml>=6.0
jsonschema>=4.17.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Code Quality
black>=23.0.0
mypy>=1.5.0
ruff>=0.1.0

# Utilities
requests>=2.31.0
aiohttp>=3.8.0
tenacity>=8.2.0  # Retry logic

# Database (PostgreSQL)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.12.0  # Database migrations
asyncpg>=0.29.0  # Async PostgreSQL driver

# API Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
textstat>=0.7.3

# Authentication & Security
python-jose[cryptography]>=3.3.0  # JWT tokens
passlib[bcrypt]>=1.7.4  # Password hashing
python-multipart>=0.0.6  # Form data
email-validator>=2.1.0  # Email validation
slowapi>=0.1.9  # Rate limiting

# Embeddings & Vector Database (Plagiarism Detection)
voyageai>=0.2.0  # Voyage AI embeddings (primary)
openai>=1.0.0  # OpenAI embeddings (fallback)
chromadb>=0.4.0  # Vector database
sentence-transformers>=2.2.0  # Local embeddings (optional)

# Text Similarity
python-Levenshtein>=0.21.0  # Edit distance for fuzzy matching
